# 明日のブラウザデモ手順

## 開始前（2分）

1. Finderで `デモを起動.command` をダブルクリックする。
2. ブラウザで `http://localhost:4173/` が開くことを確認する。
3. 右上の「デモを初期化」を押し、確認画面で実行する。
4. 画面左下に「Aさん（仮名）」、日誌件数に「24」が表示されることを確認する。

デモ中は、起動時に開いたターミナルを閉じないでください。

## おすすめの説明順（7分）

### 1. 今日の状況（1分）

- 2か月・24件の日誌が入っていることを見せる。
- 「職員確認待ち」「保護者共有準備」「計画の更新状態」を一画面で確認できると説明する。

### 2. 日誌を書く（2分）

1. 左メニュー「日誌を書く」を開く。
2. 5月29日の「雨天による予定変更」を選ぶ。
3. 左側が根拠となる原記録、右側が用途別の下書きであることを説明する。
4. 「職員確認を完了」→「保護者共有の準備へ」の順に押す。

元記録を変更した場合、古い下書きのまま共有されないよう確認状態へ戻ります。

### 3. 保護者への共有（1分）

1. 左メニュー「保護者への共有」を開く。
2. 共有前プレビューを確認する。
3. 内部の観察・支援メモは表示されず、保護者向け文章だけが表示されることを説明する。
4. 必要なら「デモ共有する」を押す。

この操作はブラウザ内の状態変更だけで、メール・LINE等へは送信しません。

### 4. 日誌をふり返る（1分）

- 5領域の記録量と前半・後半の変化を確認する。
- 1件の出来事だけで断定せず、複数日・複数場面の記録を根拠にする設計を説明する。

### 5. 個別支援計画（2分）

- 本人・家族の意向、総合方針、長期・短期目標を確認する。
- 本人支援、家族支援、移行支援、地域連携、5領域、担当、期限、評価方法を見せる。
- 各支援目標から根拠日誌へ戻れることを見せる。
- 最後に「概要PDF」と「詳細PDF」を紹介する。

## NotebookLMを併用する場合

「児童・日誌」画面の「NotebookLM用CSV」を押すと、24件の日誌を出力できます。職員記録と保護者向け文章は別列です。実在児童の情報はアップロードせず、明日のデモでは同梱の架空データだけを使ってください。

## トラブル時

- `ERR_CONNECTION_REFUSED`：`デモを起動.command` をもう一度ダブルクリックする。
- 表示が前回の状態：右上の「デモを初期化」を押す。
- 画面が更新されない：ブラウザで再読み込みする。
- 終了：起動時のターミナルで `Control + C` を押す。

## デモ上の注意

- 外部AI、保護者アカウント、メール・LINE送信は接続していない。
- 保存先はこのブラウザ内の `localStorage`。
- 個別支援計画は「見直し原案」。正式運用では面接、会議、説明、文書同意、交付が必要。
